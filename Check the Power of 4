
/*
 Because In the bit set of any no , if only even positioned bits are one then it indicates that the particular no is power of 4
*/


bool isPowerOfFour(int n) {
        for(int i=0; i<16; i++) {
            if(n == 1 << 2*i) {
                return true;
            }
        }
        return false;
 }
 

